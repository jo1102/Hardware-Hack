# Everything the PC side of Kairo needs. The board itself runs MicroPython
# and needs nothing installed.
#
#     python -m pip install -r requirements.txt

pyserial>=3.5     # site/bridge.py holds the USB serial port open
mpremote>=1.20    # copies files onto the MicroPython board
